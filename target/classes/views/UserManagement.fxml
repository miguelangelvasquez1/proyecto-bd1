<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.tienda.controller.UserManagementController"
            prefHeight="700.0" prefWidth="1000.0"
            styleClass="main-container">

    <!-- Header -->
    <top>
        <VBox spacing="15" styleClass="header">
            <padding>
                <Insets top="20" right="30" bottom="20" left="30"/>
            </padding>

            <!-- Botón Volver y Título -->
            <HBox alignment="CENTER_LEFT" spacing="20">
                <Button fx:id="btnBack" text="← Volver" 
                        onAction="#handleBack" 
                        styleClass="back-button"/>
                
                <Region HBox.hgrow="ALWAYS"/>
                
                <Label text="Gestión de Usuarios" styleClass="title">
                    <font>
                        <Font name="System Bold" size="28"/>
                    </font>
                </Label>
                
                <Region HBox.hgrow="ALWAYS"/>
                
                <Button fx:id="btnRefresh" text="🔄 Actualizar" 
                        onAction="#handleRefresh" 
                        styleClass="refresh-button"/>
            </HBox>

            <Separator/>

            <!-- Barra de búsqueda y filtros -->
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Label text="Buscar:">
                    <font>
                        <Font size="14"/>
                    </font>
                </Label>
                <TextField fx:id="txtSearch" 
                          promptText="Buscar por nombre o email..."
                          prefWidth="300"
                          styleClass="search-field"/>
                <Button text="🔍 Buscar" 
                        onAction="#handleSearch"
                        styleClass="search-button"/>
                <Button text="✖ Limpiar" 
                        onAction="#handleClearSearch"
                        styleClass="clear-button"/>
                
                <Region HBox.hgrow="ALWAYS"/>
                
                <Label fx:id="lblTotalUsers" text="Total: 0 usuarios" styleClass="info-label">
                    <font>
                        <Font name="System Bold" size="14"/>
                    </font>
                </Label>
            </HBox>
        </VBox>
    </top>

    <!-- Tabla de Usuarios -->
    <center>
        <VBox spacing="10" styleClass="content">
            <padding>
                <Insets top="10" right="30" bottom="20" left="30"/>
            </padding>

            <TableView fx:id="tableUsers" VBox.vgrow="ALWAYS" styleClass="user-table">
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="60" styleClass="table-column"/>
                    <TableColumn fx:id="colName" text="Nombre" prefWidth="180" styleClass="table-column"/>
                    <TableColumn fx:id="colEmail" text="Email" prefWidth="200" styleClass="table-column"/>
                    <TableColumn fx:id="colPhone" text="Teléfono" prefWidth="120" styleClass="table-column"/>
                    <TableColumn fx:id="colRole" text="Rol" prefWidth="130" styleClass="table-column"/>
                    <TableColumn fx:id="colActions" text="Acciones" prefWidth="150" styleClass="table-column"/>
                </columns>
                
                <placeholder>
                    <Label text="No hay usuarios para mostrar" styleClass="placeholder-label">
                        <font>
                            <Font size="16"/>
                        </font>
                    </Label>
                </placeholder>
            </TableView>
        </VBox>
    </center>

    <!-- Footer -->
    <bottom>
        <HBox alignment="CENTER" spacing="20" styleClass="footer">
            <padding>
                <Insets top="15" right="30" bottom="15" left="30"/>
            </padding>
            
            <Label text="⚠️ Advertencia: La eliminación de usuarios es permanente" 
                   styleClass="warning-label">
                <font>
                    <Font size="12"/>
                </font>
            </Label>
        </HBox>
    </bottom>

</BorderPane>